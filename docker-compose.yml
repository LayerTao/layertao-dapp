version: "3.8"

services:
  layertao-dapp:
    container_name: layertao-dapp
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    restart: always
    env_file:
      - .env
